<div class="card">
    <div class="card-body">
        <form action="/rooms/update?id={{data.room._id}}" method="post">
              <div class="mb-3">
                <label class="form-label" for="basic-icon-default-email">Role Name</label>
                <div class="input-group input-group-merge">
                    <span class="input-group-text"><i class="bx bx-envelope"></i></span>
                    <input type="text" id="basic-icon-default-fullname" name="name" 
                     value="{{data.room.name}}"
                        class="form-control" placeholder="" aria-label="john.doe"
                        aria-describedby="basic-icon-default-email2" />
                </div>
            </div>
            <button type="submit" class="btn btn-primary">update</button>
        </form>
    </div>
</div>